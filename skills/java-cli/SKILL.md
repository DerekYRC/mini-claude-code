---
name: java-cli
description: Build and run small Java command-line demos.
---

# Java CLI Skill

Use this skill when working on Java command-line demos. Prefer Java 17, Maven, small classes, and direct demo verification with `mvn package -DskipTests` plus a real run command.
